# 环境变量（含 API Key，绝不上传）
.env

# 缓存目录（本地运行缓存，不需上传）
cache/

# 生成的报告文件
*.xlsx
*.txt

# Python 缓存
__pycache__/
*.pyc
*.pyo

# Word 临时文件（以 ~$ 开头）
~$*.docx

# Claude Code 内部目录
.claude/

# 原始 Word 文档（体积大，可选不上传）
*.docx
